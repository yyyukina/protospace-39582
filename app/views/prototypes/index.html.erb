<main class="main">
  <div class="inner">
    <%# ログインしているときは以下を表示する %>
    <% if user_signed_in? %>
      <div class="greeting">
        こんにちは、
        <%= link_to "#{current_user.name}さん", user_session_path, class: :greeting__link %>
      </div>
    <%# // ログインしているときは上記を表示する %>
    <% else %>
      <!-- ログインしていないときのコード（省略） -->
    <% end %>
    <div class="card__wrapper">
    <%= render partial: 'prototype', collection: @prototypes %>
       
      <%# 投稿機能実装後、部分テンプレートでプロトタイプ投稿一覧を表示する %>
    </div>
  </div>
</main>